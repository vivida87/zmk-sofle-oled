# go to sleep after one hour (1*60*60*1000ms)

CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

CONFIG_ZMK_SLEEP=y

# Turn on logging, and set ZMK logging to debug output
# CONFIG_ZMK_USB_LOGGING=n
#
# Copyright (c) 2022 Darryl deHaan
# SPDX-License-Identifier: MIT
#
# Enable 32kHz crystal (블루투스 멈춤 원인으로 주석 처리)
# CONFIG_CLOCK_CONTROL_NRF_K32SRC_XTAL=y # <<< [수정: 주석 처리]
# CONFIG_CLOCK_CONTROL_NRF_K32SRC_30PPM=y # <<< [수정: 주석 처리]

CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=y
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=n

#The maximum brightness percentage of RGB，Reducing this value can lower the temperature of the rectifier components.(RGB的最大百分比亮度，降低可以减少发热）
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX=60

CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_USB=y

# RGB 색상 보정 (초록빛 제거 및 순수 백색 설정)
CONFIG_ZMK_RGB_UNDERGLOW_SAT_START=0 # <<< [추가: 채도를 0으로 만들어 백색 출력]
CONFIG_ZMK_RGB_UNDERGLOW_HUE_START=0 # <<< [수정: 160(청록색) -> 0으로 변경]
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=0 # <<< [수정: 3(이펙트) -> 0(단색 백색)으로 변경]


#CONFIG_ZMK_HID_CONSUMER_REPORT_USAGES_BASIC=y

#EC11 enable
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

#EXT POWER
CONFIG_ZMK_EXT_POWER=y

# MOUSE
CONFIG_ZMK_POINTING=y

CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=n

CONFIG_ZMK_BACKLIGHT=y
CONFIG_ZMK_BACKLIGHT_BRT_START=100
CONFIG_ZMK_BACKLIGHT_ON_START=y
CONFIG_ZMK_BACKLIGHT_AUTO_OFF_IDLE=n

#缩小数值减少延迟但对轴体品质要求会更高 Reducing the value will decrease the latency, but it will also place higher demands on the switching quality. If you are confident about the quality of the switch used on the keyboard, you can try smaller values, such as 1ms - 3ms.
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=10
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10

#soft off
CONFIG_ZMK_PM_SOFT_OFF=y

#enable display drivers
CONFIG_ZMK_DISPLAY=y
